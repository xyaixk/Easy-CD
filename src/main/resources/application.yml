server:
  port: 8080
  servlet:
    context-path: /api

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://public.bc.office.ysb:3306/cd_database?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: AAbb11@@
  jackson:
    time-zone: Asia/Shanghai
    date-format: yyyy-MM-dd HH:mm:ss

mybatis-plus:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.yunshaoboedu.bmp.entity
  configuration:
    map-underscore-to-camel-case: true

# OpenSearch日志检索配置
opensearch:
  uri: http://172.21.49.17:9200
  # 索引模式：{env} 占位符会被当前环境名替换
  index-pattern: logs-{env}-*
  # 单次查询上限（取“全部”时也不超过此值）
  max-result-window: 10000
